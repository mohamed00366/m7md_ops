// ⚠️ DEPRECATED — مَهجور، اِستُبدِل بِنِظام "أَمانة" في
// lib/features/laundry/camp_boss/batches_screen.dart
import 'package:flutter/material.dart';

class LaundryBatchesScreen extends StatelessWidget {
  const LaundryBatchesScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
